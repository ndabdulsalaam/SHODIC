# Django Core
SECRET_KEY=your_django_secret_key_here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
ALLOWED_ORIGINS=http://localhost:5173

# Database (Neon Postgres — neon.tech)
DATABASE_URL=

# AI — DeepSeek (LLM — platform.deepseek.com)
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com

# AI — OpenAI (Embeddings only — platform.openai.com)
OPENAI_API_KEY=

# Vector DB — Qdrant Cloud (cloud.qdrant.io)
QDRANT_URL=
QDRANT_API_KEY=

# Brevo Email API (brevo.com — for OTP delivery)
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=RxChat

# Google OAuth (optional — console.cloud.google.com)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback/
