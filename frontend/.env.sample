# Production (set in Vercel dashboard -> Environment Variables)
VITE_API_BASE_URL=https://rxchat.onrender.com/api

# Local development can omit this. RxChat will use http://localhost:8000/api
# automatically when the browser is on localhost or 127.0.0.1.
